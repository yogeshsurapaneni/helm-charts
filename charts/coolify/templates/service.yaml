apiVersion: v1
kind: Service
metadata:
  name: {{ include "coolify.fullname" . }}
spec:
  selector:
    {{- include "coolify.selectorLabels" . | nindent 4 }}
  ports:
    - port: 8080
      targetPort: 8000